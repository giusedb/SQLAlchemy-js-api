# JSAlchemAPI

### API response

```json
{
    "description": {
      "pk": ["id"],
      "description": "describe the model",
      "fields": [
        {
          "name": "field name",
          "desctiption": "describe the meaning of the field",
          "type": "string",
          "required": true,
          "default": "default value"
        }
      ]
    }
}
```
